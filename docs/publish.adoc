
```bash
pnpm release:alpha   # alpha preview
pnpm release:beta    # beta preview
pnpm release         # stable release
```

```bash
pnpm --filter mfront build
pnpm --filter mfront version:alpha
pnpm --filter mfront-core publish:alpha
```

```bash
pnpm --filter mfront release:alpha
```

== Local Publishing
* verdaccio
```
npm install -g verdaccio
```

* .npmrc
```
registry=http://localhost:4873/
always-auth=true
```

set NODE_OPTIONS=--max-old-space-size=8192